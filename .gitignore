node_modules
# npm/yarn/bun lockfiles must never exist here — this repo is pnpm-only. A
# stray package-lock.json (left behind by a one-off npm command) gets swept
# up into the EAS Build upload and flips its package-manager detection to
# npm — the lockfile resolution order checks npm before pnpm — so the
# builder runs `npm ci` against a lockfile with zero dependencies and
# installs nothing. Ignoring them keeps strays out of the upload entirely.
package-lock.json
npm-shrinkwrap.json
yarn.lock
bun.lock
bun.lockb
.env
.env.local
.env*.local
# Broader net for any other .env variant (e.g. a plain .env.production with
# no .local suffix, which the three patterns above wouldn't otherwise catch)
# at any depth — apps/web/.env.production, apps/mobile/.env.staging, etc.
# The negation keeps the actual template files (which must stay committed)
# out of this ignore.
.env*
!.env.example
!**/.env.example
dist
.next
out
.expo
*.tgz
.DS_Store
apps/web/tsconfig.tsbuildinfo
apps/marketing/tsconfig.tsbuildinfo
graphify-out
