# App screenshots for the "A look inside" section

Add three portrait phone screenshots here with these exact filenames
(the page's SHOTS array in app/page.tsx already points at them):

- `onboarding.png` — the "Introduce your dog" onboarding screen
- `share.png` — the shared cheat-sheet a stand-in opens in their browser
- `poster.png` — the "If … goes missing" poster screen

Full-height iPhone screenshots are ideal (~9:19.5). They're shown inside a
phone frame with object-cover, cropped from the top, so the header of each
screen stays visible.
