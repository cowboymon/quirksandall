# Custom iOS build config — needed ONLY because of a confirmed upstream bug
# in EAS Build's package-manager auto-detection for pnpm monorepos where the
# Expo app lives in a nested directory (apps/mobile, not the repo root).
# EAS's built-in `eas/install_node_modules` step misdetects this project as
# an npm workspace and runs `npm ci`, which installs nothing real. This is
# the same list of steps `eas/build` runs by default for an iOS build using
# EAS-managed (remote) credentials, with `eas/install_node_modules` swapped
# for a manual pnpm install. See github.com/expo/eas-cli issues #2978, #3247.
#
# CONSEQUENCE — build numbers must be local, not remote. `eas/prebuild` below
# regenerates the native project from app.json, so CFBundleVersion ends up as
# whatever app.json says. A default `eas/build` run injects the remote build
# number into the job before prebuild; this hand-rolled step list does not. So
# with appVersionSource "remote" and no ios.buildNumber in app.json, prebuild
# fell back to Expo's default of "1", and every archive shipped as
# CFBundleVersion 1 while the EAS dashboard reported 15, 16, 17. Confirmed by
# reading Info.plist out of three of the uploaded IPAs; the only one that
# carried a real number was a build made while app.json had the value pinned.
#
# eas.json therefore pins appVersionSource "local" and app.json carries
# ios.buildNumber. Do not switch it back to "remote" without also adding a step
# here that writes the version into the native project — the dashboard number
# lies otherwise, and App Store Connect rejects the upload as a duplicate.
build:
  name: iOS build with manual pnpm install (workaround for EAS pnpm-workspace detection bug)
  steps:
    - eas/checkout

    - run:
        name: Install dependencies with pnpm
        command: cd ../.. && pnpm install --frozen-lockfile

    - eas/resolve_build_config

    - eas/prebuild

    - eas/configure_eas_update:
        inputs:
          throw_if_not_configured: false

    # Not eas/install_pods: that helper locates the Podfile via the same
    # monorepo auto-detection this whole file works around, and looks in the
    # repo root instead of apps/mobile/ios. Pin the directory explicitly.
    - run:
        name: Install pods
        working_directory: ./ios
        command: pod install

    - eas/configure_ios_credentials

    - eas/generate_gymfile_from_template:
        inputs:
          credentials: ${ eas.job.secrets.buildCredentials }

    - eas/run_fastlane

    - eas/find_and_upload_build_artifacts
